import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MonadTrivia - Battle Royale Trivia on Monad Blockchain",
  description: "Real-time multiplayer Battle Royale trivia game where up to 100 players compete simultaneously. Built on Monad blockchain showcasing parallel execution. Wrong answers mean instant elimination!",
  keywords: ["Monad", "blockchain", "trivia", "battle royale", "Web3", "gaming", "cryptocurrency"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
